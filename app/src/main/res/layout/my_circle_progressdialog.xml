<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent" >

    <RelativeLayout 
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:paddingBottom="20dp"
        android:paddingLeft="30dp"
        android:paddingRight="30dp"
        android:paddingTop="20dp"
        android:background="@drawable/progress_custom_bg"
        >
        <!--加载圈的样式-->
        <ProgressBar
            android:id="@+id/my_circle_progress_pb"
            android:layout_width="60dp"
            android:layout_height="60dp"
            android:indeterminateDrawable="@drawable/progressbar"
            android:layout_centerHorizontal="true"
            android:layout_marginTop="@dimen/length_10"/>

    <TextView 
        android:id="@+id/my_circle_progress_tv"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="加载中..."
        android:textColor="#FFFFFF"
        android:layout_centerHorizontal="true"
        android:layout_below="@+id/my_circle_progress_pb"
        android:layout_marginTop="@dimen/length_2"/>
    </RelativeLayout>
    

</RelativeLayout>